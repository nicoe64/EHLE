#include "simplestack.h"
#include <iostream>

using namespace std;

int main() {

    SimpleStack1<int> st;
    int i = 1;

    if(!st = full())
        st.push(i);
    if(!st = empty())
        cout << st.top() << '\n';
}